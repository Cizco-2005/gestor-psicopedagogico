name: gestor_psicopedagogico
description: Aplicación móvil para la gestión de atención psicopedagógica.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Se pueden agregar aquí las dependencias del proyecto:
  # http, go_router, flutter_riverpod, etc.

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
